This is a tiny metadata file added to create a visible diff for the pull request.

Purpose:
- Generate a small commit on `feature/add-setup-scripts` so the branch differs from `main` and a PR can be created via the GitHub UI.

You can now open the compare page and create the PR:
https://github.com/ngjan32/tickflow-stock-panel/compare/main...feature/add-setup-scripts?expand=1
